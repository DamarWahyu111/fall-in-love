/** @type {import('next').NextConfig} */
const nextConfig = {
  // HAPUS ini kalau ada:
  // output: 'export',
  // basePath: '/fall-in-love',
  
  images: {
    unoptimized: true
  }
}

module.exports = nextConfig